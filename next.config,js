/** @type {import('next').NextConfig} */
const nextConfig = {
// Enable Turbopack for faster builds
experimental: {
turbo: true,
},
// Define the root of your project for Turbopack
turbopack: {
root: __dirname,
},
// Enable strict mode and SWC minification for optimization
reactStrictMode: true,
swcMinify: true,
// Optional: support for CSS modules if you want
webpack(config) {
config.module.rules.push({
test: /\.css$/,
use: ['style-loader', 'css-loader'],
});
return config;
},
};

module.exports = nextConfig;